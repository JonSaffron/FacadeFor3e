<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
	<!-- Must target using the -windows suffix otherwise attributes like ServiceContractAttribute are not found -->
	<TargetFrameworks>net481;netstandard2.0;net8.0-windows;net10.0-windows</TargetFrameworks>
    <PackageId>FacadeFor3E</PackageId>
    <Authors>J Saffron Consulting</Authors>
    <Product>FacadeFor3E</Product>
    <Version>5.0.0</Version>
    <Description>A library that makes integrating with Elite 3E a little easier</Description>
    <Copyright>Copyright © J Saffron Consulting Ltd 2014 - 2026</Copyright>
    <RepositoryUrl>https://github.com/JonSaffron/FacadeFor3e</RepositoryUrl>
    <EnableNETAnalyzers>true</EnableNETAnalyzers>
  </PropertyGroup>

  <PropertyGroup>
    <LangVersion>14.0</LangVersion>
    <Nullable>enable</Nullable>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>A library to help you build apps that use Elite 3E</Title>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageTags>Elite;3E;TransactionService</PackageTags>
    <AssemblyVersion>$(VersionPrefix)</AssemblyVersion>
    <FileVersion>$(VersionPrefix)</FileVersion>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="JetBrains.Annotations" Version="2026.2.0">
        <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="NLog" Version="[6,)" />

    <!-- Support for WindowsIdentity -->
    <PackageReference Include="System.Security.Principal.Windows" Version="5.0.0" />
  </ItemGroup>

  <!-- .net 8 -->
  <ItemGroup Condition="'$(TargetFramework)' == 'net8.0-windows'">
	<PackageReference Include="System.ServiceModel.Http" Version="8.1.2" />
	<PackageReference Include="System.ServiceModel.Primitives" Version="8.1.2" />

	<!-- This package is not normally referenced directly, but versions before 8.0.3 contain a vulnerability -->
	<PackageReference Include="System.Security.Cryptography.Xml" Version="[8.0.3,)" />
  </ItemGroup>

  <!-- .net 10 -->
  <ItemGroup Condition="'$(TargetFramework)' == 'net10.0-windows'">
	  <PackageReference Include="System.ServiceModel.Http" Version="10.0.652802" />
	  <PackageReference Include="System.ServiceModel.Primitives" Version="10.0.652802" />

	  <!-- This package is not normally referenced directly, but versions before 10.0.6 contain a vulnerability -->
	  <PackageReference Include="System.Security.Cryptography.Xml" Version="[10.0.6,)" />
  </ItemGroup>
  
  <!-- .net framework -->
  <ItemGroup Condition="'$(TargetFramework)' == 'net481'">
    <PackageReference Include="System.ServiceModel.Http" Version="10.0.652802" />
    <PackageReference Include="System.ServiceModel.Primitives" Version="10.0.652802" />
	<PackageReference Include="System.Net.Http" Version="4.3.4" />

    <!-- This package is not normally referenced directly, but versions before 10.0.6 contain a vulnerability -->
    <PackageReference Include="System.Security.Cryptography.Xml" Version="[10.0.6,)" />
  </ItemGroup>

  <!-- .net standard 2 -->
  <ItemGroup Condition="'$(TargetFramework)' == 'netstandard2.0'">
    <PackageReference Include="System.ServiceModel.Http" Version="10.0.652802" />
	<PackageReference Include="System.ServiceModel.Primitives" Version="10.0.652802" />
	<PackageReference Include="System.Net.Http" Version="4.3.4" />

	<!-- This package is not normally referenced directly, but versions before 10.0.6 contain a vulnerability -->
	<PackageReference Include="System.Security.Cryptography.Xml" Version="[10.0.6,)" />
  </ItemGroup>

  <ItemGroup>
    <WCFMetadata Include="Connected Services" />
  </ItemGroup>

	<ItemGroup>
		<AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
			<_Parameter1>FacadeFor3e.Tests</_Parameter1>
		</AssemblyAttribute>
	</ItemGroup>

	<ItemGroup>
	  <None Include="..\README.md">
	    <Pack>True</Pack>
	    <PackagePath>\</PackagePath>
	  </None>
    <None Include="ReleaseHistory.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Include="..\LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>
</Project>
